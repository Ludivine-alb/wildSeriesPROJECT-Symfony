{% extends 'base.html.twig' %}

{% block body %}

<div>
    {{ form_start(form) }}
</div>
    {{ form_widget(form) }}
    <div>
    <button type="submit">Add category</button>
    </div>
    {{ form_end(form) }}
{% endblock %}