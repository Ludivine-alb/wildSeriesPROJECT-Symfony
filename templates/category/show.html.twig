{% extends 'base.html.twig' %}

{% block title %}Serie by category #{{ category.id }} {% endblock %}

{% block body %}
    <div class="media">
        <h1> 3 série de la catégorie - {{ category.name }} : </h1>
        {% for program in programs %}
            <div class="media-body">
                <h2 class="mt-0"> - {{ program.id }} /{{ program.title }}</h1>
                <p> - {{ program.synopsis }}</p>
                <a class="navlink" href="{{ path('program_show',  {program : program.id}) }}">Voir la fiche</a>
            </div>
    </div>
    {% else %}
    Aucune série trouvée dans cette catégorie.
    {% endfor %}
    <a class="navlink" href="{{ path('app_index') }}">
        Retour à la page d'accueil, pas responsive haha..
    </a>
{% endblock %}