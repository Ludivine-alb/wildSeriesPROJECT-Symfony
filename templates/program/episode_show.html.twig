{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}
{{dump(episode)}}; die;
    <div class="media">
        <img class="align-self-start mr-3" src="{{ program.poster }}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1>{{ program.title }}</h1>
            <h2 class="mt-0">Episode {{ episode.number }} de la saison {{ season.number }}</h2>
            <p>Title : {{ episode.title }}</p>
            <p>Synopsis : {{ episode.synopsis }}</p>
        </div>
    </div>
    <a class="navlink" href="{{ path('program_season_show', { program: program.id, season: season.id }) }}">
        Faire demi-tour
    </a>
{% endblock %}
