{% extends 'base.html.twig' %}

{% block title %}Série #{{ season.program.id }}{% endblock %}

{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{ program.poster }}" alt="{{ season.program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">Saison {{ season.number }} de {{ season.program.title }}</h1>
            <p>{{ season.program.synopsis }}</p>
            <p>List of episodes :</p>
            {% for episode in episodes %} 
            <p><a class="navlink" href="{{ path('program_episode_show',  { programId: program.id, seasonId: season.id, episodeId: episode.id }) }}">Episode {{ episode.number }} : {{ episode.title }}</a></p>
            {% endfor %}
        </div>
    </div>

<a class="navlink" href="{{ path('program_show', { program: program.id }) }}">
        Faire demi-tour
    </a>

{% endblock %}